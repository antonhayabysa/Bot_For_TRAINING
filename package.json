{
  "name": "bot_for_training",
  "author": "Anton Snizhko",
  "funding": "",
  "license": "",
  "scripts": {
    "env": "vercel env pull .env",
    "polling": "npm run env && npm run start",
    "webhook": "npm run tunnel & vercel dev --listen 3000",
    "tunnel": "cloudflared tunnel --url http://localhost:3000",
    "start": "node --watch -r dotenv/config scripts/start.mjs",
    "test": "node --watch -r dotenv/config test.mjs"
  },
  "dependencies": {
  "@grammyjs/storage-mongodb": "^2.4.2",
  "dotenv": "^16.4.5",
  "grammy": "^1.20.4",
  "mongodb": "^6.4.0",
  "random-js": "^2.1.0",
  "vercel-grammy": "^0.2.1"
  },
  "engines": {
  "node": "^18.0.0"
  }
 }
